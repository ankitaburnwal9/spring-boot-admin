spring:
  boot:
    admin:
      client:
        url: http://localhost:8081
        username: admin
        password: admin

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always